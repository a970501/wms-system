# 更新日志 - 2026年1月14日

## 版本 7.1.0

### 🔒 安全加固

#### 后端 UserController 密码加密修复
- **问题**：管理员创建用户和重置密码时，密码以明文存储
- **修复**：启用 BCrypt 加密，与 AuthController 保持一致
- **影响文件**：`wms-backend/src/main/java/com/wms/controller/UserController.java`
- **修改内容**：
  - 添加 `BCryptPasswordEncoder` 密码编码器
  - `createUser()` 方法：使用 `passwordEncoder.encode()` 加密密码
  - `resetPassword()` 方法：使用 `passwordEncoder.encode()` 加密密码

### ✨ 新功能

#### 1. 用户修改密码功能
- **页面路径**：`pages/change-password/change-password`
- **功能描述**：允许用户自行修改登录密码
- **特性**：
  - 旧密码验证
  - 新密码强度检查（至少6位，不能是弱密码）
  - 新密码确认一致性检查
  - 密码显示/隐藏切换
  - 修改成功后自动退出登录
- **API调用**：`POST /auth/change-password`

#### 2. 管理员用户管理功能
- **页面路径**：`pages/user-management/user-management`
- **功能描述**：管理员查看和管理系统用户
- **特性**：
  - 用户列表展示（用户名、真实姓名、角色、状态）
  - 角色标签区分（管理员/普通用户/操作员）
  - 状态指示（正常/禁用）
  - 重置用户密码功能
  - 支持下拉刷新
  - 权限控制（仅管理员可访问）
- **API调用**：
  - `GET /users` - 获取用户列表
  - `PUT /users/{id}/reset-password` - 重置用户密码

### 📁 文件变更清单

#### 新增文件
```
pages/change-password/
├── change-password.js      # 修改密码页面逻辑
├── change-password.json    # 页面配置
├── change-password.wxml    # 页面结构
└── change-password.wxss    # 页面样式

pages/user-management/
├── user-management.js      # 用户管理页面逻辑
├── user-management.json    # 页面配置
├── user-management.wxml    # 页面结构
└── user-management.wxss    # 页面样式
```

#### 修改文件
| 文件 | 修改说明 |
|------|----------|
| `app.json` | 注册新页面 |
| `pages/profile/profile.js` | 添加跳转方法 |
| `pages/profile/profile.wxml` | 添加菜单入口 |
| `wms-backend/.../UserController.java` | BCrypt密码加密 |

### 🖥️ 服务器部署
- 后端代码已同步部署到服务器 `124.223.198.83`
- Maven 编译成功
- wms-backend 服务已重启

### 📱 入口位置

#### 修改密码
- 位置：我的 → 其他设置 → 🔐 修改密码
- 权限：所有登录用户

#### 用户管理
- 位置：我的 → 系统管理 → 👥 用户管理
- 权限：仅管理员可见

---

## 升级说明

### 前端
1. 拉取最新代码
2. 在微信开发者工具中刷新项目

### 后端
- 已自动部署，无需手动操作

### 数据库
- 无数据库变更

---

**更新时间**：2026-01-14 22:52  
**更新人**：System

